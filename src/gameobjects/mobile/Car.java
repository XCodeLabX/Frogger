package gameobjects.mobile;


public class Car extends Mobile {

    // ATTRIBUTES
        
    
    // CONSTRUCTORS
        public Car(int index, int length) {
            super(">");
            setIndex(index);
            setLength(length);
        }

    // DYNAMIC - METHODS
        @Override
        public void objectAction(Player player) {
            
        }

        // GETTERS
            
        
        // SETTERS
            

    // STATIC - METHODS
        
}
